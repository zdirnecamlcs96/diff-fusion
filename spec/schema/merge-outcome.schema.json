{
  "$comment": "Generated by cargo run --example gen_schema --features schema-gen. DO NOT EDIT.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MergeOutcomeWire",
  "description": "Mirrors the ad-hoc JSON built by `drivers::wire::merge_field_impl` (`{\"kind\":\"Resolved\",\"value\":...}` / `{\"kind\":\"Conflict\",\"reason\":...}`). No runtime type carries this shape — it's assembled inline with `serde_json::json!` — so this schema-only enum stands in for it.",
  "oneOf": [
    {
      "type": "object",
      "required": [
        "kind",
        "value"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "Resolved"
          ]
        },
        "value": true
      }
    },
    {
      "type": "object",
      "required": [
        "kind",
        "reason"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "Conflict"
          ]
        },
        "reason": {
          "type": "string"
        }
      }
    }
  ]
}