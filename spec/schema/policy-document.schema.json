{
  "$comment": "Generated by cargo run --example gen_schema --features schema-gen. DO NOT EDIT.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PolicyDocument",
  "description": "A whole entity type's worth of field policies, as authored by a host (e.g. a JSON document in a `jsonb` column). `entity_type` is the lookup key into a [`crate::ports::policy_store::PolicyStore`], not embedded here — mirrors [`crate::ports::ancestor::AncestorKey`] being separate from the entry it addresses.",
  "type": "object",
  "properties": {
    "default": {
      "description": "Fallback policy for paths without an explicit entry in `fields`.",
      "anyOf": [
        {
          "$ref": "#/definitions/MergePolicyRef"
        },
        {
          "type": "null"
        }
      ]
    },
    "fields": {
      "description": "Per-field policy declarations, keyed by canonical field path.",
      "default": {},
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/MergePolicyRef"
      }
    }
  },
  "definitions": {
    "MergePolicyRef": {
      "description": "Serializable declaration of a per-field merge policy.",
      "oneOf": [
        {
          "description": "One side owns the field. `system` must match `MergeContext::system_a` or `system_b` at merge time.",
          "type": "object",
          "required": [
            "kind",
            "system"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "owned_by"
              ]
            },
            "system": {
              "description": "The owning system's label.",
              "type": "string"
            }
          }
        },
        {
          "description": "Numeric counters — both sides' deltas accumulate.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "additive"
              ]
            }
          }
        },
        {
          "description": "Array fields — concatenate both sides' additions.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "append"
              ]
            }
          }
        },
        {
          "description": "Enum fields — only listed `(from, to)` transitions are allowed.",
          "type": "object",
          "required": [
            "kind",
            "transitions"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "state_machine"
              ]
            },
            "transitions": {
              "description": "The allowed `(from, to)` transitions.",
              "type": "array",
              "items": {
                "$ref": "#/definitions/TransitionRef"
              }
            }
          }
        },
        {
          "description": "Escape hatch — requires a written reason and per-cycle timestamps. Never a schema's default strategy; see [`crate::application::policy::escape_hatch`] for rationale.",
          "type": "object",
          "required": [
            "kind",
            "reason",
            "timestamp_a",
            "timestamp_b"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "last_write_wins"
              ]
            },
            "reason": {
              "description": "Written justification for using this escape hatch; surfaces in logs and conflict reports.",
              "type": "string"
            },
            "timestamp_a": {
              "description": "Millisecond epoch for the A-side value.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "timestamp_b": {
              "description": "Millisecond epoch for the B-side value.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            }
          }
        },
        {
          "description": "Keyed-set structural merge for arrays of objects — the full per-side add/remove/union configuration from [`crate::application::policy::structural::SetByKey`]. Only `identity`/`a_anchor`/`b_anchor` are required; the rest default to [`SetByKey::new`]'s defaults.",
          "type": "object",
          "required": [
            "a_anchor",
            "b_anchor",
            "identity",
            "kind"
          ],
          "properties": {
            "a_anchor": {
              "description": "Stable A-side row identifier used to rehome a row before identity matching.",
              "type": "string"
            },
            "b_anchor": {
              "description": "Same as `a_anchor` but for the B side.",
              "type": "string"
            },
            "identity": {
              "description": "Business fields forming the cross-system identity.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "kind": {
              "type": "string",
              "enum": [
                "set_by_key"
              ]
            },
            "nested": {
              "description": "Per-field nested policies, recursively merged. See [`SetByKeyRef`].",
              "default": {},
              "type": "object",
              "additionalProperties": {
                "$ref": "#/definitions/SetByKeyRef"
              }
            },
            "on_added_in_a": {
              "default": "Include",
              "anyOf": [
                {
                  "$ref": "#/definitions/OnAdded"
                },
                {
                  "type": "null"
                }
              ]
            },
            "on_added_in_b": {
              "default": "Include",
              "anyOf": [
                {
                  "$ref": "#/definitions/OnAdded"
                },
                {
                  "type": "null"
                }
              ]
            },
            "on_both_changed": {
              "default": "Escalate",
              "anyOf": [
                {
                  "$ref": "#/definitions/OnBothChanged"
                },
                {
                  "type": "null"
                }
              ]
            },
            "on_removed_in_a": {
              "default": "EscalateIfChanged",
              "anyOf": [
                {
                  "$ref": "#/definitions/OnRemoved"
                },
                {
                  "type": "null"
                }
              ]
            },
            "on_removed_in_b": {
              "default": "EscalateIfChanged",
              "anyOf": [
                {
                  "$ref": "#/definitions/OnRemoved"
                },
                {
                  "type": "null"
                }
              ]
            },
            "prefer_a_on_field_conflict": {
              "default": true,
              "type": "boolean"
            }
          }
        }
      ]
    },
    "OnAdded": {
      "type": "string",
      "enum": [
        "Include",
        "Exclude"
      ]
    },
    "OnBothChanged": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "Escalate",
            "PreferA",
            "PreferB"
          ]
        },
        {
          "description": "Shallow-union the two elements' fields. A-side wins on direct field clashes unless `prefer_a_on_field_conflict` is set to false.",
          "type": "string",
          "enum": [
            "Union"
          ]
        }
      ]
    },
    "OnRemoved": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "Remove"
          ]
        },
        {
          "description": "Remove only if the other side did not modify the same element. Modified-and-removed surfaces as a conflict.",
          "type": "string",
          "enum": [
            "EscalateIfChanged"
          ]
        }
      ]
    },
    "SetByKeyRef": {
      "description": "Satellite of [`MergePolicyRef::SetByKey`] used for its recursive `nested` field — a `SetByKey` declaration can't recurse into itself as an enum variant, so `nested` policies are this flat struct instead. Same fields, same defaults; converts to the runtime [`crate::application::policy::structural::SetByKey`] via [`From`].",
      "type": "object",
      "required": [
        "a_anchor",
        "b_anchor",
        "identity"
      ],
      "properties": {
        "a_anchor": {
          "description": "Stable A-side row identifier used to rehome a row before identity matching.",
          "type": "string"
        },
        "b_anchor": {
          "description": "Same as `a_anchor` but for the B side.",
          "type": "string"
        },
        "identity": {
          "description": "Business fields forming the cross-system identity.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "nested": {
          "description": "Per-field nested policies. A field named here is recursively merged as its own keyed array rather than shallow-overlaid.",
          "default": {},
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/SetByKeyRef"
          }
        },
        "on_added_in_a": {
          "default": "Include",
          "anyOf": [
            {
              "$ref": "#/definitions/OnAdded"
            },
            {
              "type": "null"
            }
          ]
        },
        "on_added_in_b": {
          "default": "Include",
          "anyOf": [
            {
              "$ref": "#/definitions/OnAdded"
            },
            {
              "type": "null"
            }
          ]
        },
        "on_both_changed": {
          "default": "Escalate",
          "anyOf": [
            {
              "$ref": "#/definitions/OnBothChanged"
            },
            {
              "type": "null"
            }
          ]
        },
        "on_removed_in_a": {
          "default": "EscalateIfChanged",
          "anyOf": [
            {
              "$ref": "#/definitions/OnRemoved"
            },
            {
              "type": "null"
            }
          ]
        },
        "on_removed_in_b": {
          "default": "EscalateIfChanged",
          "anyOf": [
            {
              "$ref": "#/definitions/OnRemoved"
            },
            {
              "type": "null"
            }
          ]
        },
        "prefer_a_on_field_conflict": {
          "default": true,
          "type": "boolean"
        }
      }
    },
    "TransitionRef": {
      "description": "Flat serde shape for a single `(from, to)` allowed transition.",
      "type": "object",
      "required": [
        "from",
        "to"
      ],
      "properties": {
        "from": {
          "description": "State the transition starts from.",
          "type": "string"
        },
        "to": {
          "description": "State the transition moves to.",
          "type": "string"
        }
      }
    }
  }
}