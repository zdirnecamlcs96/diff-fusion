server {
    listen 80;
    root /usr/share/nginx/html;

    # GitHub Pages resolves /foo to /foo.html automatically; plain nginx does
    # not. Mirror that so the sidebar's extensionless links work in preview.
    location / {
        try_files $uri $uri.html $uri/ $uri/index.html =404;
    }
}
